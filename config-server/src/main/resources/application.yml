server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: 
            - file:./config
            - file:../config
            - file:../../config
            - file:../../../config
            - file:${user.home}/.library-config
